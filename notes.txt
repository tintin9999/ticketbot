- wl should work
- msg/commands.ts has support for guild users / roles / channels now
- client/index.ts has big changes over how commands are taken into the map
- ready.ts removed invisible status
- commands/index.ts just imports the loader instead of the map
- create.ts now supports guild insertion
- help now ignores the util category
- toggle-guild = ads / removes the guild from db [working]
- wl.ts validates entity -> inserts into that part of document in guilds table
- eval category changed, ignore that changed
- Database/index.ts added guilds as collection
- In genericTable made genericEntity's id number | string instead of number only
- Guilds.ts the table for guilds
- ticket.ts added support for guild property
- ticket renderer now shows the ticket guild + set a & b's _id property as number 